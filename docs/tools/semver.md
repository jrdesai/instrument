# Semver

Parse [Semantic Versioning](https://semver.org/) strings, compare two versions with correct precedence (not string sorting), test whether a version satisfies a requirement string (including `^` ranges), and copy suggested next major, minor, or patch versions.

A single leading `v` on the version is accepted and stripped before parsing.
