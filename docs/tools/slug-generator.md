# Slug Generator

Transliterates text to ASCII with `deunicode`, optionally lowercases, collapses non-alphanumeric runs to a chosen separator (`-`, `_`, or `.`), trims edges, and optionally truncates to a maximum length.
