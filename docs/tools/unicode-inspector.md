# Unicode Inspector

Inspects each Unicode scalar in the input: decimal codepoint, `U+` hex, character name (Unicode data), block, a simple category label, and UTF-8 bytes (raw and hex).

Input is limited to 1000 characters per run.
