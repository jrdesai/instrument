# HTTP Status Codes

Static reference for standard HTTP status codes (1xx–5xx): official-style names, short descriptions, and quick copy for numeric codes. Runs entirely in the UI with no backend or network calls.

**Features:** search across code, name, and description; filter by class (1xx–5xx); section groupings when viewing all classes.
