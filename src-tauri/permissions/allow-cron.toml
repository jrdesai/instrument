# Allow the Cron Expression Parser tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-cron"
description = "Allows invoking the tool_cron_process command."
commands.allow = ["tool_cron_process"]
