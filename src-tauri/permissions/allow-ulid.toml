# Allow the ULID Generator tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-ulid"
description = "Enables the ulid_process and ulid_inspect commands."
commands.allow = ["ulid_process", "ulid_inspect"]

