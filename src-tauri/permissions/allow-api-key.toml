# Allow the API Key Generator tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-api-key"
description = "Enables the tool_api_key_process command."
commands.allow = ["tool_api_key_process"]

