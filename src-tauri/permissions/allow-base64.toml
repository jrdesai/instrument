# Allow the Base64 tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-base64"
description = "Enables the base64_process command for the Base64 encoder tool."
commands.allow = ["base64_process"]
