# Allow the HTML formatter tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-html-format"
description = "Enables the tool_html_format command."
commands.allow = ["tool_html_format"]
