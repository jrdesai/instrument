# Allow the combined Hash tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-hash"
description = "Enables the tool_hash_process command."
commands.allow = ["tool_hash_process"]
