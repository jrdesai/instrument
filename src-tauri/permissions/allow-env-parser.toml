# Allow the .env Parser command to be invoked from the frontend.
[[permission]]
identifier = "allow-env-parser"
description = "Enables the tool_env_parse command."
commands.allow = ["tool_env_parse"]
