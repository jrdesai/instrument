# Allow the JSON Schema Validator tool command to be invoked from the frontend.
[[permission]]
identifier = "allow-json-schema-validator"
description = "Enables the tool_json_schema_validate command."
commands.allow = ["tool_json_schema_validate"]
